"""Route Agent — deterministic spatial-intelligence & network-topology engine.

Models the global crude-oil supply network as an extensible weighted graph and
recomputes the optimal corridor to India when nodes/links are disrupted (either
by live News/Risk signals or by manual war-gaming clicks).
"""

__version__ = "0.1.0"
